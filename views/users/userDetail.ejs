<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/stylesUserDetail.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <%- include('../partials/head.ejs') %>
    <title>Detalle de Usuario</title>
</head>
<body>
    <%- include('../partials/header.ejs') %>
    <main>
        <div class="detail-box">
            <div>
                <a href="/user-edit/<%= user.id %>">Editar Usuario</a>
            </div>
            <div>
                <form action="" method="post">
                    Borrar Usuario
                    <button type="submit"></button>
                </form>
            </div>
            <div class="titles">Mi Usuario</div>
            <div class="img-box">
                <img src="/images/users/<%= user.image %>" alt="NoResponse" srcset="">
            </div>
            <div class="text-box">
                <div class="titles">Nombre</div>
                <label for="name" class="info"><%= user.name %></label>
                <div class="titles">Email</div>
                <label for="email" class="info"><%= user.email %></label>
                <div class="titles">Fecha de Nacimiento</div>
                <label for="birhtdate" class="info"><%= user.birthdate %></label>
            </div>
        </div>
    </main>
    <%- include('../partials/footer.ejs') %>
</body>
</html>